services:
  ecoplate-blue:
    image: ${APP_IMAGE}:${IMAGE_TAG:-latest}
    container_name: ecoplate-blue
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATABASE_PATH=/app/data/ecoplate.db
      - RECOMMENDATION_ENGINE_URL=http://recommendation-blue:5000
    volumes:
      - ecoplate-data:/app/data
    depends_on:
      recommendation-blue:
        condition: service_healthy
    networks:
      - ecoplate-blue-net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  recommendation-blue:
    image: ${REC_IMAGE}:${IMAGE_TAG:-latest}
    container_name: recommendation-blue
    restart: unless-stopped
    ports:
      - "5001:5000"
    environment:
      - PORT=5000
      - FLASK_ENV=production
    networks:
      - ecoplate-blue-net
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  ecoplate-blue-net:
    driver: bridge

volumes:
  ecoplate-data:
    external: true
